name: reference-standalone
description: Minimal lifecycle demo (no live orders)
# Domain (spot/perp/…) comes from the connector MarketType, not this file.
exchanges:
  - hyperliquid
assets:
  hyperliquid:
    - base: BTC
      quote: USD
parameters:
  dry_run: true
